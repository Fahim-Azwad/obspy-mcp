# Core seismology
obspy==1.4.2

# Numeric / plotting
numpy==2.0.2
matplotlib==3.9.4

# MCP runtime (includes pydantic>=2 + pydantic-settings)
mcp==1.25.0

# Env loading (used by config)
python-dotenv==1.2.1

# Google Gemini SDK (new, supported package)
google-genai==1.2.0

# HTTP stack (often already pulled, but pin for stability)
httpx==0.28.1
anyio==4.12.0

